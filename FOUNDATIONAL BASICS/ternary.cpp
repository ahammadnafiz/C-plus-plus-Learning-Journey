#include <iostream>


int main(){

/*
ternary operator syntax:
    condition ? expression1 : expression2;

    here ?: = replace if else statement

*/
    // int grade;
    // bool result;
    // std::cout << "Enter Your Grade: ";
    // std::cin >> grade;

    // result = grade >= 60;

    // std::cout << (result ? "You Passed" : "You Failed") << '\n';

    // Even Odd

    // int num;
    // std::cout << "Enter a Number: ";
    // std::cin >> num;

    // std::cout << ((num % 2 == 0) ? "Even" : "Odd") << '\n';

    bool hungry = true;

    std::cout << (hungry ? "GO EAT" : "Do your work") << '\n';

    return 0;
}